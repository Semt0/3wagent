name: 3wagent-refactor
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - qwen-agent[gui,python-executor]>=0.0.34
      - json5>=0.9.0
      - numpy>=1.24.0
      - soundfile>=0.12.0
      - socksio>=1.0.0
      - ruff>=0.5.0
      - pytest>=8.0
