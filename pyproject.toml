[project]
name = "3wagent"
version = "0.1.0"
description = "Claude Code/Codex skills for cross-border policy research."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
  "ruff>=0.5.0",
  "pyyaml>=6.0",
  "jsonschema>=4.0",
  "pytest>=8.0",
]
mcp = [
  "mcp>=1.0.0",
  "pyyaml>=6.0",
]
server = [
  "fastapi>=0.100",
  "mcp>=1.0.0",
  "pyyaml>=6.0",
  "uvicorn>=0.20",
]

[tool.ruff]
line-length = 100
